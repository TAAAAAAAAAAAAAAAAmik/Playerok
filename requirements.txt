# python-telegram-bot 20.x не работает на Python 3.13+: его Updater объявлен
# со __slots__, а новый Python запрещает дописывать туда атрибут, и бот падает
# ещё на сборке приложения — «'Updater' object has no attribute
# '_Updater__polling_cleanup_cb'». Начиная с 21.0 это исправлено.
#
# Нижнюю границу держим, верхнюю ограничиваем мажорной версией: ломающие
# изменения приходят с ней, и лучше узнать о них при обновлении, чем на сервере.
python-telegram-bot>=21.6,<23
# httpx не закрепляем: его версию выбирает сама библиотека, и жёсткий пин
# 0.25.2 (нужный для 20.7) теперь только мешает разрешить зависимости.
python-dotenv>=1.0.0
aiofiles>=23.2.1
selenium>=4.27.1
curl_cffi>=0.7.4
