# python-telegram-bot задаёт допустимый диапазон httpx
# (22.8 -> httpx>=0.27,<0.29), поэтому эти две версии обновляются только вместе.
python-telegram-bot==22.8
httpx==0.28.1
python-dotenv==1.2.2

# Эндпоинт Playerok принимает только persisted queries: клиент отправляет имя
# операции и sha256-хэш, а не текст запроса, — поэтому запросы нельзя написать
# вручную. PlayerokAPI хранит эти хэши и подменяет TLS-отпечаток для DDoS-Guard.
# Версия не прибита намеренно: когда Playerok меняет хэши операций, нужна свежая.
PlayerokAPI @ git+https://github.com/alleexxeeyy/PlayerokAPI.git
# tqdm — незаявленная зависимость tls_requests внутри PlayerokAPI: без неё
# импорт падает с ModuleNotFoundError.
tqdm
